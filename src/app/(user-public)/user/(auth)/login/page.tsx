import { LoginForm } from "~/app/(user-public)/user/(auth)/login/_components/login-form";

export default function LoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center px-4">
      <LoginForm />
    </main>
  );
}
