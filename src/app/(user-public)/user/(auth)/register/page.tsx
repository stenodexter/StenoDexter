import { RegisterForm } from "~/app/(user-public)/user/(auth)/register/_components/register-form";

export default function RegisterPage() {
  return (
    <main className="flex min-h-screen items-center justify-center px-4 bg-card">
      <RegisterForm />
    </main>
  );
}
